spring.application.name=api-gymnasio

server.port=8080

# Conexión a la base de datos MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/gymnasioBD?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=diego@outlook.com
spring.datasource.password=123456

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=validate   #para crear7alterar en flyway
spring.jpa.open-in-view=false
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Flyway
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

# (Opcional) logs SQL parametrizados
# logging.level.org.hibernate.SQL=debug
# logging.level.org.hibernate.type.descriptor.sql.BasicBinder=trace
app.jwt.secret=Edeliza
app.jwt.expiration-ms=86400000
